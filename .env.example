# Gemini runs through Vertex AI over Application Default Credentials — no API
# key. Authenticate once with: gcloud auth application-default login
#
# Optional overrides
# GCP_PROJECT=songbook-generator
# GCP_LOCATION=us-central1
# GEMINI_MODEL=gemini-2.5-flash
# BUCKET_BASE_URL=https://storage.googleapis.com/ukulele-tuesday-songbooks

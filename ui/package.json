{
  "name": "setlister-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:emulator": "npx --yes firebase-tools@15.26.0 emulators:exec --only firestore --config ../firebase.json --project demo-setlister \"vitest run\""
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "lucide": "^1.24.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
